import React from 'react';

const NoPage = () => (
  <div>
    <h2 style = {{textAlign: "center", fontFamily: "monospace", color: "red"}}>(404)</h2>
    <h2 style = {{textAlign: "center", fontFamily: "monospace"}}>page not found</h2>
  </div>
)

export default NoPage;